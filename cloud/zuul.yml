server:
  port: 80

ribbon:
  okhttp:
    enabled: true

zuul:
  ignoredServices: '*'
  sensitiveHeaders: Cookie,Set-Cookie,Authorization
  routes:
    users:
      path: /api/**
      serviceId: consumer
      stripPrefix: true