server:
  port: 8007

ribbon:
  okhttp:
    enabled: true

zuul:
  ignoredServices: '*'
  sensitiveHeaders: Cookie,Set-Cookie,Authorization
  routes:
    consumer:
      path: /a/**
      serviceId: consumer
      stripPrefix: true
    user:
      path: /b/**
      serviceId: consumer
      stripPrefix: true