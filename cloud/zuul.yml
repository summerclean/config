server:
  port: 80

ribbon:
  okhttp:
    enabled: true

zuul:
  ignoredServices: '*'
  sensitiveHeaders: Cookie,Set-Cookie,Authorization
  routes:
    consumer:
      path: /api/**
      serviceId: consumer
      stripPrefix: true
    user:
      path: /abc/**
      serviceId: consumer
      stripPrefix: true